import { Deserializable } from './deserializable.module';
import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

// @NgModule({
//   declarations: [],
//   imports: [CommonModule],
// })

export interface Player {
  steamid: string;
  // communityvisibilitystate!: number;
  // profilestate!: number;
  realname: string;
  // commentpermission!: number;
  // profileurl!: string;
  // avatar!: string;
  // avatarmedium!: string;
  // avatarfull!: string;
  // avatarhash!: string;
  // lastlogoff!: number;
  // personastate!: number;
  // realname!: string;
  // primaryclanid!: string;
  // timecreated!: number;
  // personastateflags!: number;
  // loccountrycode!: string;
  // locstatecode!: string;

  // deserialize(input: any): this {
  //   Object.assign(this, input);
  //   return this;
  // }
}
